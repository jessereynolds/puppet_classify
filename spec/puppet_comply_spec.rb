require "spec_helper"

RSpec.describe PuppetComply do
  it "has a version number" do
    expect(PuppetComply::VERSION).not_to be nil
  end

  it "produces online text" do
    expect(false).to eq(true)
  end
end
